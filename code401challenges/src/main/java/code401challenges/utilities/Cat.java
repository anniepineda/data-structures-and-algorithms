package code401challenges.utilities;

public class Cat extends Animal{
    //creating a class called cat that takes in a string name as a parameter and calls name from the super class animal.
    public Cat(String name) {super(name);}
}
